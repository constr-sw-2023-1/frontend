import { SxProps } from "@mui/material";
import { minWidth } from "@mui/system";

export const textField: SxProps = {
    borderRadius: '5px',
    boxShadow: '0 1px 2px rgba(77,77,77,.1)',
    minWidth: '300px',
} 